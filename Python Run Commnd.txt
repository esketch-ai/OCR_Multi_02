# 1. 기본 실행 (설정 파일의 INPUT_DIR인 'Data' 폴더 사용)
# 모듈 실행 방식(-m src.main)을 권장합니다.
python3 -m src.main

# 2. 특정 대용량 폴더 지정 실행 예시
# --input_dir 뒤에 실제 폴더 경로를 입력하세요.
# 예: Data 폴더가 현재 위치에 있다면:
python3 -m src.main --input_dir Data